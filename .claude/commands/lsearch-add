#!/bin/bash
# lsearch-add: Add temporary path to knowledge base

if [ -z "$1" ]; then
    echo "Usage: /lsearch-add <path>"
    echo "Example: /lsearch-add ./docs"
    exit 1
fi

cd "${PWD}"
lsearch add-path "$1" 2>/dev/null || echo "Error: lsearch not installed. Run: pip install lsearch"
