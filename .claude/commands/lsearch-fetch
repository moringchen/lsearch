#!/bin/bash
# lsearch-fetch: Fetch URL to knowledge base

if [ -z "$1" ]; then
    echo "Usage: /lsearch-fetch <url>"
    echo "Example: /lsearch-fetch https://docs.example.com"
    exit 1
fi

cd "${PWD}"
python -m lsearch.fetcher "$1" 2>/dev/null || echo "Error: Failed to fetch URL"
